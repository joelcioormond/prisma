# Dependências principais
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
#flask-cors==4.0.0
Flask-CORS==4.0.0
Werkzeug==3.1.3

# Banco de dados
# Banco de dados - versão compatível com Windows
# Para PostgreSQL no Windows, use uma dessas opções:
# Opção 1: Instalar PostgreSQL binário primeiro, depois:
#psycopg2-binary==2.9.7  # PostgreSQL adapter

# 
# Opção 2: Usar apenas SQLite (mais simples para desenvolvimento)
# (sem psycopg2)

# Geração de PDF
reportlab==4.0.4
weasyprint==60.0

# Utilitários
python-dotenv==1.0.0  # Carregamento de variáveis de ambiente
gunicorn==21.2.0      # WSGI server para produção

# Desenvolvimento (opcional)
pytest==7.4.2
pytest-flask==1.2.0
